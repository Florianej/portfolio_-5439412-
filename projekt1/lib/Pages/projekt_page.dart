import 'package:flutter/material.dart';

class ProjektPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Projekt'),
      ),
      body: Center(
        child: Text('Projekt Page'),
    ),
);
}
}